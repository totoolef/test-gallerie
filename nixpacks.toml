[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "stdenv"]

[phases.install]
cmds = [
    # Utiliser requirements-cloud.txt qui exclut streamlit et matplotlib
    "pip install --break-system-packages --no-cache-dir -r requirements-cloud.txt",
    # Nettoyer les caches pip pour réduire la taille
    "pip cache purge || true"
]

[phases.build]
cmds = []

[start]
cmd = "./start_server.sh"

